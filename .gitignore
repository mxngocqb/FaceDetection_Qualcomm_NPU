# Build output (rebuild via scripts/build.sh)
/build/
*.o
*.obj

# Stale build binaries (if built at root)
/face_det_lite
/cam_detect_web
/cam_yolo_web
/yolo_inspect

# Model compile temp
model_compile/calib/
*_single.onnx
*.rgb

# Runtime/temp
*.log
cam_*.jpg

# Keep: src/*.c, scripts/*.sh, third_party/, models/*.bin, assets/, docs/, *.md
